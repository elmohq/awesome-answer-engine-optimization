# URLs the weekly link check skips, one per line.
#
# Keep this file as short as humanly possible. It exists for links that cannot
# be checked automatically, not for links that are broken. Every line needs a
# comment saying why.
#
# Currently empty, which is how it should stay.
